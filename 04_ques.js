//adding the elements at the one end and removing the elements at others end this is known as the queues
const a=[1,2,3,4]
a.unshift(10) //adding the elements at the begining
a.pop() //removing the elememts at the last
console.log(a)